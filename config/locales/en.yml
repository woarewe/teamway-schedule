en:
  rest:
    api:
      organizations:
        shifts:
          errors:
            long_period: "The retrieving period should be <= %{period} days"
    services:
      authentication:
        errors:
          missing_header: Header "%{header}" is missing
          invalid_format: >
            Invalid payload for the header %{header}
            Please, use the following format: "%{scheme} Base64(USERNAME:PASSWORD)
          invalid_creds: Invalid username or password
    validation:
      errors:
        not_found: not found
  models:
    shift:
      errors:
        double_booking: "The worker already has a shift on %{date}"
        past_start: is in the past

  hello: "Hello world"
